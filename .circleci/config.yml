version: 1
jobs:
  unittest:
    docker:
      - image: circleci/node:16.10.0
    steps:
      - checkout
      - run: yarn install
      - run: yarn test

workflows:
  version: 1
  test:
    jobs:
      - unittest
